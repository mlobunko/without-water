import React from 'react';

export const CounterNewsComponent = ({ total }) => (
  <div className="counter-news">
    <span>Total News: {total}</span>
  </div>
);

export default CounterNewsComponent;
